__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
dist/
build/
*.egg-info/
.env
.env.*
!.env.example
*.db
*.sqlite*
state/
models/
checkpoints/

# Isolated git worktrees for delegated builds. Each holds a .git file pointing
# back here, so `git add -A` would sweep a whole nested repo copy into a commit.
.claude/worktrees/
